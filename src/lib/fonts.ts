/*
 * ════════════════════════════════════════════════════════════════
 *  שלושת הפונטים — מאוחסנים אצלנו, לא אצל גוגל
 *
 *  היה כאן next/font/google. הוא עבד, אבל הוריד את הפונטים מגוגל
 *  **בכל בנייה**, ובאחת הריצות (23.09) גוגל לא הייתה זמינה מהראנר
 *  של GitHub והבנייה נפלה על שלושה `Failed to fetch`.
 *
 *  עכשיו הקבצים יושבים ב-public/fonts/ ונכנסים לגיט.
 *  כללי ה-@font-face ב-src/styles/fonts.css, שנוצר אוטומטית על ידי
 *  scripts/fetch-fonts.ps1 — כולל unicode-range לכל תת-קבוצה, כך
 *  שדפדפן שמציג רק עברית לא מוריד את הקובץ הלטיני וההפך.
 *
 *  אין יותר שום בקשה לגוגל: לא בבנייה, ולא בזמן ריצה.
 *
 *  להוספת משקל: להוסיף אותו ל-$families ב-fetch-fonts.ps1,
 *  להריץ את הסקריפט, ולקמט את הקבצים החדשים.
 * ════════════════════════════════════════════════════════════════
 */

/**
 * הפונטים שנטענים ראשונים ולכן שווים preload.
 * רק עברית — הטקסט באתר עברי, והקובץ הלטיני נטען רק כשצריך אותו.
 * שלושתם ביחד ~31KB.
 */
export const preloadedFonts = [
  '/fonts/rubik-700-hebrew.woff2', // כותרות הסקשנים
  '/fonts/heebo-400-hebrew.woff2', // גוף הטקסט
  '/fonts/frank-ruhl-700-hebrew.woff2', // שם הסטודיו בהדר
] as const

/**
 * נשאר כדי לא לשבור קריאות קיימות. משתני ה-CSS מוגדרים עכשיו
 * על :root ב-fonts.css, ולא כ-class על ה-html.
 */
export const fontVariables = ''
